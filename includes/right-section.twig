<div class="right-section container">

    <!-- Page Content -->
    <section class="template-content">

        <div class="companyname-container">
            {{widget('companyname', 'companyname', {"business": "profile"})|raw}}
        </div>

        <div class="headline-container">
            {{widget('content', 'content-h2', {
                content: '<h2>FLOWER SHOP OWNER</h2>'
            })|raw}}
        </div>

        <div class="socialicons-container">
            {{widget('profile', 'socialicons', {
              'profileType':'socialicons',
              'twitter':'profile',
              'refreshtime':'1800000'
            })|raw}}
        </div>

        <div class="companydescription-container">
            {{widget('content', 'content-p', {
                content: '<p>Type content here...</p>'
            })|raw}}
        </div>

        <div class="button-container">
            {{widget('button', 'button', {
                'text': 'View My Portfolio'
            })|raw}}
        </div>


        {% include "includes/footer.twig" %}

        <div class="main-zone">

            <!-- Main Widet Zone -->
            {{zone('main')|raw}}

        </div>

    </section>
    <!-- End Page Content -->

</div>
